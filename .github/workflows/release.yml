name: release

# Phase 0 placeholder for contextify-hooks. Real release pipeline lands when the
# package's first feature release ships.
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  noop:
    name: release placeholder
    runs-on: ubuntu-latest
    steps:
      - run: echo "contextify-hooks release placeholder; wires up in a later phase"
